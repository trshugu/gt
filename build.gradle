buildscript {
  repositories {
    mavenCentral()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:0.4'
  }
}

apply plugin: 'java'
apply plugin: 'android'

android {
  compileSdkVersion 17
}